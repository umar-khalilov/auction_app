'use strict';

const NODE_ENV = Object.freeze({
    DEVELOPMENT: 'development',
    PRODUCTION: 'production',
});

const SortOrders = Object.freeze({
    ASC: 'ASC',
    DESC: 'DESC',
});

export { NODE_ENV, SortOrders };
